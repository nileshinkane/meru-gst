# Database migrations

Only schema migrations belong in this directory.

Do not commit customer, invoice, medicine, or other business data. Local seed
files and SQLite database files are ignored by the root `.gitignore`.
